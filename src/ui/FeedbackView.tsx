import React, { useState } from 'react';
export function FeedbackView({ onSubmit }: { onSubmit: (kind: string, text: string) => void }) { const [kind, setKind] = useState('idea'); const [text, setText] = useState(''); return <div className="view"><div className="view-heading"><div><p className="eyebrow">FEEDBACK · LOCAL DRAFT</p><h1>Feedback</h1><p className="muted">Nada é enviado automaticamente.</p></div></div><section className="settings-card feedback-form" aria-label="Feedback form"><label>Type<select value={kind} onChange={(event) => setKind(event.target.value)}><option value="idea">Idea</option><option value="bug">Bug</option><option value="feedback">Feedback</option></select></label><label>Details<textarea value={text} onChange={(event) => setText(event.target.value)} placeholder="Describe what you noticed..." rows={8} /></label><button className="primary" disabled={!text.trim()} onClick={() => { onSubmit(kind, text.trim()); setText(''); }}>Save locally</button></section></div>; }

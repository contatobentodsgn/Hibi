import React from 'react';
import { durationMinutes, toDateKey } from '../domain/schedule';
import type { ScheduleBlock, StudyData } from '../domain/models';
type Props = { data: StudyData; onEvent: (action: string, detail: string, result?: string) => void; onCreateBlock: (input: Omit<ScheduleBlock, 'id'>) => void };
const WEEK_START = '2026-09-07'; const OFFSET = '-03:00'; const labels = ['SUN','MON','TUE','WED','THU','FRI','SAT'];
const addDays = (key: string, amount: number) => { const date = new Date(`${key}T12:00:00${OFFSET}`); date.setDate(date.getDate() + amount); return date.toISOString().slice(0, 10); };
const tone = (category: ScheduleBlock['category']) => category === 'break' ? 'green' : category === 'learning' ? 'blue' : 'orange';
export function WeekView({ data, onEvent, onCreateBlock }: Props) { const days = Array.from({ length: 7 }, (_, index) => addDays(WEEK_START, index)); const addBlock = (date: string, hour: number) => onCreateBlock({ title: 'Quick study block', start: `${date}T${String(hour).padStart(2, '0')}:00:00${OFFSET}`, end: `${date}T${String(hour + 1).padStart(2, '0')}:00:00${OFFSET}`, category: 'work' }); const exportIcs = () => { const esc = (text: string) => text.replace(/[\\;,]/g, '\\$&').replace(/\n/g, '\\n'); const events = data.blocks.map((block) => `BEGIN:VEVENT\nUID:${block.id}@hibi\nDTSTART:${block.start.replace(/[-:]/g, '').replace('.000', '')}\nDTEND:${block.end.replace(/[-:]/g, '').replace('.000', '')}\nSUMMARY:${esc(block.title)}\nEND:VEVENT`).join('\n'); const blob = new Blob([`BEGIN:VCALENDAR\nVERSION:2.0\nPRODID:-//Hibi//EN\n${events}\nEND:VCALENDAR`], { type: 'text/calendar' }); const url = URL.createObjectURL(blob); const link = document.createElement('a'); link.href = url; link.download = 'hibi-calendar.ics'; link.click(); URL.revokeObjectURL(url); onEvent('export', 'Exported calendar ICS', 'pass'); }; return <div className="view calendar-view"><div className="view-heading"><div><p className="eyebrow">WEEKLY PLAN · SEPTEMBER 2026</p><h1>Mon 07 — Sun 13</h1><p className="muted">Work windows, protected time and recurring reminders</p></div><div className="heading-actions"><button className="outline" onClick={exportIcs}>Export .ics</button><button className="primary" onClick={() => { addBlock(WEEK_START, 8); onEvent('create', 'Quick add to weekly calendar'); }}>+ Add</button></div></div><div className="calendar-toolbar"><button className="filter active">All layers</button><button className="filter">Schedule</button><button className="filter">Important</button><button className="filter">Wellbeing</button></div><div className="week-grid"><div className="week-corner">TIME</div>{days.map((date) => <div className="day-head" key={date}>{labels[new Date(`${date}T12:00:00${OFFSET}`).getDay()]}<b>{date.slice(8, 10)}</b></div>)}{Array.from({ length: 15 }, (_, index) => index + 8).map((hour) => <React.Fragment key={hour}><div className="time-label">{hour}:00</div>{days.map((date) => { const block = data.blocks.find((item) => toDateKey(item.start) === date && Number(item.start.slice(11, 13)) === hour); return <div className="week-slot" key={`${date}-${hour}`} onClick={() => { addBlock(date, hour); onEvent('quick-add', `${date} ${hour}:00`); }}>{block && <div className={`mini-card ${tone(block.category)}`}>{block.title}<span>{durationMinutes(block) / 60}h</span></div>}</div>; })}</React.Fragment>)}</div><div className="conflict-inline"><span>✓</span><strong>No hard conflicts detected</strong><span className="muted">Protected lunch, walking and classes are respected.</span></div></div>; }

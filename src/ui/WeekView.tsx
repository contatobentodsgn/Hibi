import React from 'react';
type Props = { onEvent: (action: string, detail: string, result?: string) => void };
const days = ['MON 03', 'TUE 04', 'WED 05', 'THU 06', 'FRI 07', 'SAT 08', 'SUN 09'];
export function WeekView({ onEvent }: Props) { return <div className="view calendar-view"><div className="view-heading"><div><p className="eyebrow">WEEKLY PLAN · AUGUST 2026</p><h1>Mon 03 — Sun 09</h1><p className="muted">Work windows, protected time and recurring reminders</p></div><button className="primary" onClick={() => onEvent('create', 'Quick add to weekly calendar')}>+ Add</button></div><div className="calendar-toolbar"><button className="filter active">All layers</button><button className="filter">Schedule</button><button className="filter">Important</button><button className="filter">Wellbeing</button><span className="spacer" /><button className="filter">‹</button><button className="filter">›</button></div><div className="week-grid"><div className="week-corner">TIME</div>{days.map((day) => <div className="day-head" key={day}>{day.split(' ')[0]}<b>{day.split(' ')[1]}</b></div>)}{[9, 10, 11, 12, 13, 14, 15, 16, 17].map((hour) => <React.Fragment key={hour}><div className="time-label">{hour}:00</div>{days.map((day, dayIndex) => <div className="week-slot" key={`${day}-${hour}`} onClick={() => onEvent('quick-add', `${day} ${hour}:00`)}>{dayIndex < 5 && (hour === 9 || hour === 10 || hour === 11 || hour === 14 || hour === 15 || hour === 16) && <div className="mini-card orange">Post {hour === 9 ? '1' : hour === 10 ? '2' : hour === 11 ? '3' : hour - 10} <span>1h</span></div>}{dayIndex < 5 && hour === 12 && <div className="mini-card green">Lunch <span>2h</span></div>}{dayIndex < 5 && hour === 17 && <div className="mini-card blue">Walk <span>2h</span></div>}</div>)}</React.Fragment>)}</div><div className="conflict-inline"><span>✓</span><strong>No hard conflicts detected</strong><span className="muted">Protected lunch, walking and classes are respected.</span></div></div>; }
